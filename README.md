# DevStudio — Marketing Site

Next.js 14 single-page marketing site scaffolded for a dark, premium agency website. Built with Tailwind CSS and Framer Motion.

Install:

```bash
npm install
npm run dev
```

Deploy: Ready for Vercel — push repository and configure build command `npm run build`.
